folder : `.env`

port = 5000

## Available Scripts
In the project directory, you can run:
backend Start = `npm start`

## Available Scripts
In the project directory, you can run:
frontend = `npm start`